事前にコマンドプロンプトでコマンドの存在を確認。
npm -v
バージョンが表示されなければ、下記からインストールする。
https://nodejs.org/en

npm init
npm install obniz 

npm install puppeteer
npm install node-cron

実行するときは、上記の実行環境にコマンドラインで移動。
「node xx.js」コマンドで実行できる。

プログラムを強制終了するときはctrl+cを実行。
